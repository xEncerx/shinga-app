name: storage
description: "Storage client for Dart."
version: 0.1.0

environment:
  sdk: ">=3.11.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  flutter_secure_storage: ^10.3.1
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  shared_preferences: ^2.5.5
